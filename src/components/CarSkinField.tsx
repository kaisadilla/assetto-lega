import React from 'react';

export interface CarSkinFieldProps {
    
}

function CarSkinField (props: CarSkinFieldProps) {

    return (
        <div>
            
        </div>
    );
}

export default CarSkinField;
